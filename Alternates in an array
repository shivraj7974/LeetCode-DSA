class Solution {
  public:
    // arr[] is the array
    void print(vector<int> &arr) {
        // code here
          int N =arr.size();
        for(int i=0; i<N;i++)
        {
            if(i%2==0)
            { 
             cout<<arr[i]<<" ";
            }
        }
    }
